# Copy this file to .env and fill in your real key.
# .env is git-ignored - never commit real secrets.

# Required for PDF skill extraction (JD / resume uploads in Section 1 and 2).
# The app still runs without it - only the "Upload PDF" tabs need it.
OPENAI_API_KEY=sk-...
